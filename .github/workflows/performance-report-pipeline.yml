name: LRE Performance Report – Azure Foundry + Slack

on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled'

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is currently disabled."
